ANTHROPIC_API_KEY=your_key_here
CORS_ORIGINS=https://your-vercel-url.vercel.app,http://localhost:3000

# Optional, only needed for pipeline/02_fetch_satellite_methane.py and
# pipeline/03_fetch_nighttime_lights.py once you have credentials — see
# the docstring in each script for free registration steps.
EARTHDATA_TOKEN=
EOG_USER=
EOG_PASS=

# Defaults to /tmp/svcii.db if unset.
DATABASE_URL=/tmp/svcii.db
